name: Code check

on:
  push:
    branches: [ master ]

jobs:
  my-testing:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Запустить мою программу
        run: myprog