name: Code check

on:
  push:
    branches: [ main ]

jobs:
  my-testing:



    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Запустить мою программу
        run: myprog